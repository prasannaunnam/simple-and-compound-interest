#include<stdio.h>
main()
{
	float a,principal,rate,time;
	printf("principal,rate,time");
	scanf("%f%f%f",&principal,&rate,&time);
	a=(principal*rate*time)/100;
	a=principal*pow((1+rate/100),time);
	printf("%f",a);
}
